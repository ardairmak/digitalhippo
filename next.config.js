/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "https://digitalhippo-production-1a0a.up.railway.app/",
    ],
  },
};

module.exports = nextConfig;
